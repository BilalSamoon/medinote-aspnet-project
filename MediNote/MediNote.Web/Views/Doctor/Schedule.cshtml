@model MediNote.Web.ViewModels.DoctorScheduleViewModel

@{
    ViewData["Title"] = "Doctor Schedule";
}

<div class="container mt-4">
    <h1 class="mb-3">Doctor Schedule</h1>
    <p class="text-muted">
        This page allows the doctor to view scheduled appointments.
    </p>
    <p class="text-muted">
        <strong>Doctor:</strong> @Model.DoctorName
    </p>
    <p class="text-muted">
        <strong>Schedule Date:</strong> @Model.ScheduleDate.ToString("yyyy-MM-dd")
    </p>

    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            Today's Appointments
        </div>
        <div class="card-body">
            <table class="table table-striped">
                <thead>
                    <tr>
                        @if (User.IsInRole("Admin"))
                        {
                            <th>Doctor</th>
                        }
                        <th>Patient</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Priority</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var appointment in Model.Appointments)
                    {
                        <tr>
                            @if (User.IsInRole("Admin"))
                            {
                                <td>@appointment.DoctorName</td>
                            }
                            <td>@appointment.PatientName</td>
                            <td>@appointment.AppointmentDate.ToString("yyyy-MM-dd")</td>
                            <td>@appointment.AppointmentTime</td>
                            <td>
                                @if(appointment.Priority == "HIGH") { <span class="badge bg-danger">HIGH</span> }
                                else if(appointment.Priority == "MEDIUM") { <span class="badge bg-warning text-dark">MEDIUM</span> }
                                else { <span class="badge bg-success">@appointment.Priority</span> }
                            </td>
                            <td>@appointment.Status</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>